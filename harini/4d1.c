#include<stdio.h>
#include<string.h>
int main()
{
    char username[10]="harini";
    int password=2936;
    char arr[10];
    int x;
    if(((("%s%d",username,arr)==0)) &&(password==x))
    printf("true");
    else
    printf("false");
    return 0;
}